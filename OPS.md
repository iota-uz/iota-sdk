- [ ] 2026-09-06 · prod · Configure the publisher App in `iota-uz/iota-sdk`, npm trust, and SDK tag/branch rules before the first release request — `SDK_APP_ID`, `SDK_APP_PRIVATE_KEY`; protect `v*`; require `Release contract and tooling` from `.github/workflows/test.yml` on SDK `main`. See [rollout](docs/sdk-releases.md#recovery-and-rollout).
- [ ] 2026-09-06 · prod · Adopt the verified-release workflow in each participating consumer and disable competing SHA finalizers — install `.github/workflows/sdk-dependency.yml`, separate consumer/requester App credentials, and require the `sdk/release` commit status in that consumer repository as described in [consumer setup](docs/sdk-releases.md#install-the-consumer-workflow).
