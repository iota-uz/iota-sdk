// Package clienthost owns the standard framework-neutral client route bootstrap contract.
package clienthost

import "github.com/iota-uz/iota-sdk/pkg/sdkidentity"

const ProtocolVersion = sdkidentity.ProtocolVersion

const BootstrapVersion = "1.0.0"
