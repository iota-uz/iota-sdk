package {{ .Name }}

import (
	"context"
)

type Repository interface {
	Count(ctx context.Context) (int64, error)
	GetAll(ctx context.Context) ([]*{{ .Type }}, error)
	GetPaginated(ctx context.Context, limit, offset int, sortBy []string) ([]*{{ .Type }}, error)
	GetByID(ctx context.Context, id int64) (*{{ .Type }}, error)
	Create(ctx context.Context, upload *{{ .Type }}) error
	Update(ctx context.Context, upload *{{ .Type }}) error
	Delete(ctx context.Context, id int64) error
}
