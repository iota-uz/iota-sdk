version: "2"

run:
  timeout: 10000s
  allow-parallel-runners: true

issues:
  exclude-use-default: false
  exclude-files:
    - pkg/graphql/graphql.go
  exclude-dirs:
    - pkg/fp
    - postgres-data
    - py-embed
    - docs
    - logs
    - e2e
    - migrations
  exclude-rules:
    # Allow nil comparison for controllers that may return nil in production builds
    - path: "modules/core/module\\.go"
      text: "SA4023"
      linters:
        - staticcheck
    # Dev CLI: allow fmt for user-facing output
    - path: "cmd/dev/main\\.go"
      linters:
        - forbidigo
output:
  format: colored-line-number
  sort-results: true
  sort-order:
    - linter
    - severity
    - file

linters:
  settings:
    exhaustruct:
      include:
        - "^github\\.com/iota-uz/iota-sdk/modules/.*"
      exclude:
        - ".*LocalizeConfig$"
        - ".*i18n\\.Message$"
        - ".*url\\.URL$"
    staticcheck:
      checks:
        - "all"
        - "-SA4023"  # Allow "always true" comparisons for build-tagged nil checks
        - "-ST1000"  # Package comment style issues are currently tolerated in legacy packages
        - "-ST1003"  # Legacy acronym/casing names are currently tolerated
        - "-ST1016"  # Legacy receiver-name inconsistencies are currently tolerated
        - "-ST1020"  # Legacy doc comment wording style is currently tolerated
        - "-ST1021"  # Legacy exported-type comment wording style is currently tolerated
        - "-ST1022"  # Legacy exported-var comment wording style is currently tolerated
  exclusions:
    rules:
      - path-except: "modules/.*/(domain/(entities|aggregates)/.*|infrastructure/persistence/.*_mappers\\.go|infrastructure/persistence/mappers/.*\\.go|presentation/mappers/.*\\.go)"
        linters:
          - exhaustruct
      - path: ".*_test\\.go"
        linters:
          - exhaustruct
  enable:
    - asasalint
    - asciicheck
    - bidichk
    - bodyclose
    - canonicalheader
    #    - containedctx
    #    - contextcheck
    - copyloopvar
    - decorder
    - dogsled
    #    - dupword
    - durationcheck
    - errcheck
    - errchkjson
    - errname
    - exhaustive
    - exhaustruct
    - fatcontext
    - forbidigo
    #    - gci
    - ginkgolinter
    - gocheckcompilerdirectives
    # - gochecknoinits  # Disabled: we use init() for build-tagged conditional feature registration
    - gochecksumtype
    #    - gocognit
    #    - goconst
    #    - gocyclo
    #    - godot
    #    - gofmt
    #    - gofumpt
    - goheader
    #    - goimports
    - gomoddirectives
    - gomodguard
    - goprintffuncname
    #    - gosimple
    #    - gosmopolitan
    - govet
    - grouper
    - importas
    - ineffassign
    #    - intrange
    #    - lll
    - loggercheck
    #    - maintidx
    - makezero
    - mirror
    - misspell
    - musttag
    - nakedret
    #    - nestif
    - nilerr
    - nilnil
    - noctx
    - nolintlint
    - nonamedreturns
    - nosprintfhostport
    #    - perfsprint
    - prealloc
    - predeclared
    - promlinter
    - protogetter
    - reassign
    #    - revive
    - rowserrcheck
    - sloglint
    - spancheck
    - sqlclosecheck
    - staticcheck
    #    - stylecheck
    - testableexamples
    - testifylint
    - thelper
    - unconvert
    #    - unparam
    - unused
    - usestdlibvars
    - wastedassign
    - whitespace
    - zerologlint
