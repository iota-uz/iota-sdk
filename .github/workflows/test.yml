name: Test & Build

on:
  - push

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Check out code into the Go module directory
        uses: actions/checkout@v2

      - name: Build, test && lint
        run: docker compose -f docker-compose.testing.yml up --build --abort-on-container-exit

