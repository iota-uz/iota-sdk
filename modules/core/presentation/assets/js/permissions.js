/**
 * Permissions script stub.
 * Loaded by the base layout. Applications can replace this asset
 * with client-side permission checks (e.g. Alpine data) if needed.
 */
(function () { 'use strict'; })();
